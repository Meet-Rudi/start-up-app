# LOCAL DEV/TEST ONLY — not bundled into the Lambda.
# Windows has no system IANA tz database, so `zoneinfo` needs this package to run the
# quiet-hours tests locally. On AWS Lambda the managed Python runtime already ships the
# system tz database, so no runtime dependency is required there.
#   python -m pip install -r services/whatsapp/tests/requirements-dev.txt
tzdata
