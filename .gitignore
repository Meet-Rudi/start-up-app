# --- AWS SAM build/deploy artifacts ---
.aws-sam/
samconfig.toml

# --- Secrets / credentials (NEVER commit) ---
*.env
.env*
*.pem
*.key
credentials.csv
*accessKeys*.csv
.aws/
# Operator console runtime config holds the interim console token — keep it local only.
console/config.js

# --- GDPR / compliance working docs (internal only, NEVER commit) ---
# Drafts, RoPA, DPIA, processor register, breach log etc. live here until
# legal sign-off. Keep out of git so internal compliance notes never leak.
gdpr-matters/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / front-end ---
node_modules/
dist/
build/

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- meetrudi-tts build artifacts (regenerated by build_layer.py / seed_voices.py) ---
services/tts/layer/
services/tts/.voice-cache/
services/tts/.token

# --- voice comparison samples (regenerated by services/tts/generate_samples.py) ---
_voice_samples/

# Deployment settings for `python deploy.py` — phone numbers, sender addresses, the WhatsApp
# join phrase. Not secrets, but not for git either (CLAUDE.md §0.5). Copy
# deploy.local.example.json to get started.
deploy.local.json
